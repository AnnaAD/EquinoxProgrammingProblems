
public class ReturnFoundCat {
	public static void main(String args[]) {
		
	}
	
	public static int[] solution(float[] catCoors, float[][] cityCoors) {
		float[] distances = new float[cityCoors.length];
		
		float smallestVal =  (float) Math.hypot((catCoors[0] - cityCoors[i][0]), (catCoors[1] - cityCoors[i][1]));
		int smallestIndex = 0;
		for(int i = 0; i < distances.length; i++) {
			distances[i] = (float) Math.hypot((catCoors[0] - cityCoors[i][0]), (catCoors[1] - cityCoors[i][1]));
		}
		
		
	}
}
